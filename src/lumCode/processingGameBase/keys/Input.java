package lumCode.processingGameBase.keys;

/**
 * Enum to define the names of keys of the game. Should not be 'KEYBOARD_A' but
 * rather 'WEAPON_1' or 'INVENTORY', so not the key itself but a function a user
 * should be able to perform with a simple keystroke.
 * 
 * @author Lumme
 */

public enum Input {

	LEFT, RIGHT, UP, DOWN, INTERACT;
}
